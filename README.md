# TryCatchBlockFinder-Crawler-


Finds missing try catch blocks in each C# method and displays them in the console. 
Can be used against a whole project directory. Used for exception handling purposes.
